# door_designer 项目记忆库

> 最后更新: 2025-11-04
> 分析状态: 已完成初始化分析

---

## 项目核心信息

### 基本信息
- **项目名称**: door_designer (门设计器)
- **项目类型**: Flutter 跨平台应用
- **当前版本**: 1.0.0+1
- **开发状态**: 初始化阶段 (仅有标准 Flutter 模板代码)
- **Git 分支**: master
- **初始提交**: 541cd2c "Initial commit"
- **项目路径**: /home/kang/git/door_designer

### 项目定位
根据项目名称推测，该项目预期为**门窗设计应用**，可能用于:
- 室内/室外门窗的可视化设计
- 门窗尺寸和材质选择工具
- 设计预览和渲染
- 订单管理和报价系统

**重要**: 目前项目仅为 Flutter 默认模板，尚未实现任何业务功能。

---

## 技术架构

### 技术栈
| 组件 | 技术 | 版本 |
|------|------|------|
| 编程语言 | Dart | ^3.8.1 |
| 框架 | Flutter | >=3.18.0-18.0.pre.54 |
| UI 系统 | Material Design 3 | - |
| 状态管理 | setState (原生) | - |
| 代码规范 | flutter_lints | ^5.0.0 |
| 图标库 | cupertino_icons | ^1.0.8 |

### 支持平台
完整的六平台支持配置:
- 📱 **移动端**: Android (Gradle/Kotlin DSL), iOS (Xcode)
- 💻 **桌面端**: Windows (CMake), macOS (Xcode), Linux (CMake)
- 🌐 **Web**: Flutter Web + PWA 支持

### 构建工具
- **Android**: Gradle (build.gradle.kts)
- **iOS/macOS**: Xcode (xcodeproj)
- **Windows/Linux**: CMake (CMakeLists.txt)
- **Web**: Flutter Web Engine

---

## 项目结构

### 目录组织
```
door_designer/
├── .claude/                      # Claude AI 项目记忆和配置
│   └── PROJECT_MEMORY.md         # 本文件
├── .dart_tool/                   # Dart 工具生成文件
├── .idea/                        # IntelliJ IDEA 配置
├── android/                      # Android 平台代码
│   ├── app/
│   ├── gradle/
│   ├── build.gradle.kts
│   └── settings.gradle.kts
├── ios/                          # iOS 平台代码
│   └── Runner/
├── web/                          # Web 平台代码
│   ├── index.html
│   ├── manifest.json
│   └── favicon.png
├── macos/                        # macOS 平台代码
├── linux/                        # Linux 平台代码
├── windows/                      # Windows 平台代码
├── lib/                          # Dart 源代码 (主要开发目录)
│   └── main.dart                 # 应用入口文件
├── test/                         # 测试文件
│   └── widget_test.dart          # Widget 测试
├── build/                        # 构建输出目录 (git ignored)
├── pubspec.yaml                  # 依赖配置文件
├── pubspec.lock                  # 依赖锁定文件
├── analysis_options.yaml         # Dart 分析器配置
├── README.md                     # 项目说明文档
├── .gitignore                    # Git 忽略规则
└── .metadata                     # Flutter 项目元数据
```

### 源代码文件清单
当前仅有 3 个 Dart 文件:
1. `lib/main.dart` - 应用入口和主界面 (123 行)
2. `test/widget_test.dart` - Widget 测试 (31 行)
3. `.dart_tool/dartpad/web_plugin_registrant.dart` - 自动生成

**重要**: 这是一个几乎空白的项目，核心业务代码尚未开始开发。

---

## 当前代码实现

### 应用架构
```
MyApp (StatelessWidget)                     # lib/main.dart:7-36
├── MaterialApp
│   ├── theme: ColorScheme.fromSeed(Colors.deepPurple)
│   └── home: MyHomePage
│
MyHomePage (StatefulWidget)                 # lib/main.dart:38-54
├── title: String (配置属性)
│
_MyHomePageState (State<MyHomePage>)        # lib/main.dart:56-122
├── _counter: int                           # 状态变量
├── _incrementCounter(): void               # 业务方法
└── build(): Widget                         # UI 构建
    ├── AppBar (紫色标题栏)
    ├── Center > Column
    │   ├── Text("You have pushed the button...")
    │   └── Text(_counter) - 大号显示
    └── FloatingActionButton (+ 图标)
```

### 核心功能
**当前功能**: 计数器演示应用 (Flutter 默认模板)
- 显示按钮点击次数
- 点击悬浮按钮增加计数
- 使用 `setState()` 更新 UI
- Material Design 3 主题

**测试覆盖**:
- `widget_test.dart` 验证计数器从 0 开始并可递增

---

## 依赖管理

### 生产依赖
```yaml
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8      # iOS 风格图标
```

### 开发依赖
```yaml
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0        # 代码规范检查
```

### 未配置的资源
- ❌ 图片资源 (assets/images)
- ❌ 自定义字体 (fonts)
- ❌ 本地化文件 (l10n)
- ❌ 配置文件 (.env)

---

## 开发环境

### 配置文件
- `analysis_options.yaml` - 启用 flutter_lints 推荐规则
- `pubspec.yaml` - 配置为私有包 (publish_to: 'none')
- `.gitignore` - 标准 Flutter 忽略规则

### IDE 支持
- ✅ IntelliJ IDEA / Android Studio (已配置 .idea/)
- ⚪ VS Code (未配置，可添加)

### Flutter 配置
- Material Design: ✅ 已启用
- 热重载: ✅ 支持
- Web 支持: ✅ 已配置

---

## 项目状态评估

### 优势
✅ 完整的跨平台配置，支持 6 大平台
✅ 遵循 Flutter 官方项目结构规范
✅ 代码质量工具已配置 (flutter_lints)
✅ 基础测试框架已就绪
✅ Git 仓库已初始化
✅ 干净的代码库，无遗留代码

### 当前限制
⚠️ **无业务逻辑** - 仅有 Flutter 模板代码
⚠️ **无状态管理** - 未集成 Provider/Bloc/Riverpod
⚠️ **无路由系统** - 未配置 go_router 或其他路由
⚠️ **无数据持久化** - 未集成数据库 (sqflite/hive)
⚠️ **无网络层** - 未配置 HTTP 客户端 (dio/http)
⚠️ **无 UI 组件库** - 未引入第三方 UI 库
⚠️ **无国际化** - 未配置多语言支持

### 技术债务
- 无

---

## 后续开发建议

### 架构升级路线图

#### 第一阶段: 基础架构搭建
1. **状态管理**
   - 推荐: Riverpod 2.x (类型安全，无 context 依赖)
   - 备选: Bloc (适合复杂业务逻辑)

2. **路由管理**
   - 推荐: go_router (声明式路由，深度链接支持)

3. **依赖注入**
   - 推荐: get_it (服务定位器模式)

4. **网络层**
   - 推荐: dio + retrofit (类型安全的 API 调用)

5. **数据持久化**
   - 本地数据库: sqflite / drift
   - 轻量存储: shared_preferences / hive

#### 第二阶段: 门窗设计核心功能
1. **图形绘制引擎**
   - CustomPaint + Canvas API
   - 或考虑 flutter_svg 用于矢量图形
   - 可能需要 vector_math 进行几何计算

2. **设计工具模块**
   - 尺寸测量和标注系统
   - 材质选择器 (Material/Wood/Glass 等)
   - 颜色选择器 (flutter_colorpicker)
   - 图层管理系统

3. **数据模型**
   ```dart
   // 门窗设计数据模型示例
   class DoorDesign {
     String id;
     DoorType type;
     Dimensions dimensions;
     Material material;
     Color color;
     List<Component> components;
     DateTime createdAt;
   }
   ```

4. **预览和渲染**
   - 2D 平面图预览
   - 可能的 3D 渲染 (考虑 flutter_cube 或 model_viewer)

5. **导出功能**
   - PDF 导出 (pdf package)
   - 图片导出 (image package)
   - JSON 数据导出

#### 第三阶段: 业务功能
1. **用户系统** (如需要)
2. **项目管理**
3. **订单和报价**
4. **云同步** (Firebase 或自建后端)

### 推荐的包 (Pub.dev)

**核心架构**:
```yaml
dependencies:
  # 状态管理
  flutter_riverpod: ^2.4.0

  # 路由
  go_router: ^13.0.0

  # 依赖注入
  get_it: ^7.6.0

  # 网络
  dio: ^5.4.0
  retrofit: ^4.0.0

  # 数据持久化
  sqflite: ^2.3.0
  shared_preferences: ^2.2.0

  # 图形和UI
  flutter_svg: ^2.0.0
  vector_math: ^2.1.4

  # 工具类
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  # 代码生成
  build_runner: ^2.4.0
  freezed: ^2.4.5
  json_serializable: ^6.7.0
  retrofit_generator: ^8.0.0
```

---

## 关键代码位置

| 功能 | 文件路径 | 行数 |
|------|---------|-----|
| 应用入口 | lib/main.dart:3-5 | main() |
| 根 Widget | lib/main.dart:7-36 | MyApp |
| 主页面 | lib/main.dart:38-122 | MyHomePage |
| Widget 测试 | test/widget_test.dart:14-29 | Counter test |

---

## 命名约定和代码风格

### 当前代码风格
- 类名: PascalCase (MyApp, MyHomePage)
- 私有类: _PascalCase (_MyHomePageState)
- 变量名: camelCase (_counter, title)
- 私有变量: _camelCase (_counter, _incrementCounter)
- 常量: const 构造函数广泛使用

### Lint 规则
使用 flutter_lints 5.0.0 推荐规则集

---

## Git 工作流

### 分支策略
- 当前: master (默认主分支)
- 建议: 采用 Git Flow 或 GitHub Flow

### Commit 历史
```
541cd2c - Initial commit (初始提交)
```

---

## 重要注意事项

### 安全性
- ✅ pubspec.yaml 已设置 `publish_to: 'none'` (私有包)
- ⚠️ 敏感配置文件 (.env) 尚未创建
- ⚠️ API 密钥管理机制未建立

### 性能
- 当前应用极简，无性能问题
- 后续需注意:
  - Canvas 绘制性能优化
  - 大型设计文件的内存管理
  - 图片资源的懒加载

### 兼容性
- Flutter SDK 要求: >=3.18.0-18.0.pre.54
- Dart SDK 要求: ^3.8.1
- 目标所有主流平台

---

## 常见问题 (FAQ)

### Q: 项目为什么没有业务代码?
A: 这是刚创建的 Flutter 项目，仅包含官方模板代码。实际的门窗设计功能尚未开始开发。

### Q: 应该使用哪个状态管理方案?
A: 推荐 Riverpod，原因:
- 编译时类型安全
- 无需 BuildContext
- 支持代码生成
- 活跃的社区维护

### Q: 如何开始开发门窗设计功能?
A: 建议步骤:
1. 设计数据模型 (Door, Window, Component)
2. 创建基础 UI 框架 (HomePage, DesignCanvas, ToolBar)
3. 实现 CustomPainter 绘制基本图形
4. 添加交互功能 (拖拽、缩放、旋转)
5. 实现保存和加载功能

### Q: 项目名称由来?
A: 从项目名 "door_designer" 推测是门窗设计工具，但创建者尚未实现具体功能。

---

## 项目时间线

- **2025-11-04**: 项目初始化，完成 Flutter 模板创建
- **2025-11-04**: Claude AI 完成项目分析和记忆文件创建

---

## 记忆更新日志

### v1.0 - 2025-11-04
- 完成项目初始分析
- 创建项目记忆文件
- 记录技术栈和架构信息
- 制定后续开发建议

---

## 联系信息

- **项目路径**: /home/kang/git/door_designer
- **Git 仓库**: 本地仓库 (未配置远程)
- **文档位置**: .claude/PROJECT_MEMORY.md

---

*本文件由 Claude AI 自动生成并维护，用于在多次对话中保持项目上下文连续性。*
